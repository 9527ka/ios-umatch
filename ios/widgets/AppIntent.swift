//
//  AppIntent.swift
//  widgets
//
//  Intentionally empty for UMatch v1.
//  The countdown widget uses StaticConfiguration (no user-configurable
//  AppIntent), so the template's ConfigurationAppIntent (iOS 16+ AppIntents)
//  is not needed. Re-add it here (guarded with @available(iOS 17.0, *)) if a
//  configurable widget is introduced later.
//
